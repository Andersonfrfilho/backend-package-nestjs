---
"@adatechnology/logger": patch
---

feat: add initTracing() and extractAmqpContext() for centralized observability — providers for OTel tracing init and AMQP context extraction; add TracingConfig to LoggerConfig
