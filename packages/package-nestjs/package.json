{
  "name": "@backend/package-nestjs",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json -w",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@nestjs/common": "^11",
    "@nestjs/core": "^11"
  },
  "dependencies": {
    "@backend/shared": "workspace:*"
  }
}