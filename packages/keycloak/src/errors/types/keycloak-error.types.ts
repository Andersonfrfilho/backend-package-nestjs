export type KeycloakErrorOptions = {
  statusCode?: number;
  details?: unknown;
  keycloakError?: string;
};

export type ExtractedKeycloakErrorInfo = {
  statusCode?: number;
  details?: unknown;
  keycloakError?: string;
};
